#!/usr/bin/env python3
"""Type annotated function, concatenates two strings and
returns concatenated string"""


def concat(str1: str, str2: str) -> str:
    """Concatenates two strings and returns result"""
    return str1 + str2
